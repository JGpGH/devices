{
    "config": {
        "BAUDRATE": {
            "description": "speed of the serial port",
            "type": "i32",
            "default": 115200,
            "required": true
        }
    },
    "procedures": [
        "bool test(u8 ok)"
    ]
}